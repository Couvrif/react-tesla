import PropTypes from 'prop-types'
import React, { memo } from 'react'
import { ShopWrapper } from './style'

const Shop = memo((props) => {
  return <ShopWrapper>Shop</ShopWrapper>
})

Shop.propTypes = {}

export default Shop
