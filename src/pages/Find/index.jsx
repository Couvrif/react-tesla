import PropTypes from 'prop-types'
import React, { memo } from 'react'
import { FindWrapper } from './style'

const Find = memo((props) => {
  return <FindWrapper>find</FindWrapper>
})

Find.propTypes = {}

export default Find
