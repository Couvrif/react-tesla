import PropTypes from 'prop-types'
import React, { memo } from 'react'
import { FindInfoWrapper } from './style'

const FindInfo = memo((props) => {
  return <FindInfoWrapper>FindInfo</FindInfoWrapper>
})

FindInfo.propTypes = {}

export default FindInfo
