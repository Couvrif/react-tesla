import PropTypes from 'prop-types'
import React, { memo } from 'react'
import { TesMapWrapper } from './style'

const TesMap = memo((props) => {
  return <TesMapWrapper>TesMap</TesMapWrapper>
})

TesMap.propTypes = {}

export default TesMap
